node_modules/
.env
.env.*
intel-agent/.keypair.json
deplex-state.json

# local run state / logs -- not secret, just not meant to be tracked
attack/.run-count.json
*.log
*.legacy-bak

# encrypted secrets store for the Telegram bot's /setkey command -- ciphertext
# only (see src/botsecrets.mjs), but the whole point is it's still not meant
# to be tracked
bot-secrets.enc.json
